# 要发生的架构改动

梳理当前 JobConfig → runner → pipeline_executor 的调用链，确认配置解析、默认值、Reader/Writer 创建、并发参数和
  Pipeline 启动分别落在哪一层。然后给出一个不破坏现有行为的 Planner 切分方案:

• 重新分析后，之前的“注册初始化问题”基本已经解决，但 Planner 阶段确实还没有开始真正落地。

  当前实际调用链仍然是：

  JobConfig
     ↓
  start_task
     ├── ReaderRegistry::prepare_reader
     ├── WriterRegistry::prepare_writer
     ├── reader.split()
     ├── dispatch_runner()
     └── executor.run(..., JobConfig)
                           ↓
                    pipeline_executor::start_run
                           ↓
                    解析 JobConfig
                    创建 RecordBuilder
                    启动 Pipeline

  当前最大的耦合点有三个。

  ## 1. TaskExecutor 仍然接收完整 JobConfig

  当前 trait：

  pub trait TaskExecutor: Send + Sync {
      async fn run(
          &self,
          reader: Arc<dyn DataReader>,
          writer: Arc<dyn DataWriter>,
          job_config: Arc<JobConfig>,
          cancel_token: CancellationToken,
      ) -> Result<RunResult>;
  }

  这说明 Engine 仍然知道：

  JobConfig

  而且 BatchRunner 和 StreamRunner 都把它继续传给：

  start_run(..., job_config, ...)

  这与目标：

  JobConfig → ExecutionPlan → Engine

  不一致。

  Engine 后续应该接收：

  ExecutionPlan

  而不是：

  Arc<JobConfig>

  ## 2. pipeline_executor.rs 仍然解析 JobConfig

  当前：

  pub async fn start_run(
      config: PipelineConfig,
      reader: Arc<dyn DataReader>,
      writer: Arc<dyn DataWriter>,
      job_config: Arc<relus_common::JobConfig>,
      cancel_token: CancellationToken,
  ) -> Result<PipelineStats>

  内部还在做：

  let source_type = job_config
      .source
      .source_type
      .parse::<SourceType>()?;

  let record_builder = Arc::new(
      RecordBuilder::new(
          job_config.column_mapping.clone(),
          job_config.column_types.clone(),
      )?
      .with_source_type(source_type),
  );

  这里就是“配置解析侵入执行引擎”。

  pipeline_executor 不应该知道：

  - JobConfig
  - source_type
  - column_mapping
  - column_types
  - 系统配置读取逻辑

  它只应该接收已经构建好的：

  PipelineConfig
  RecordBuilder
  Reader
  Writer

  ## 3. 当前 Reader 被 split 了两次

  现在 start_task 中先执行一次：

  let split_result = reader.split(runner_config.reader_threads).await?;
  let stream_mode = split_result.stream_mode;

  但是之后进入 pipeline_executor::run_paired_pipeline，又执行一次：

  let reader_split = reader.split(config.reader_threads).await?;

  这会带来实际问题：

  - 数据库分片查询可能执行两次
  - API Reader 可能请求两次
  - Binlog Reader 可能创建两次位点或连接
  - 第一次 split 得到的任务数量没有真正传给 Pipeline
  - Planner 将来必须避免重复 split

  这比简单搬移 RecordBuilder 更重要。

  ———

  # 建议的第一版 Planner

  建议新增：

  core/src/core/planner.rs

  核心类型：

  pub struct ExecutionPlan {
      pub reader: Arc<dyn DataReader>,
      pub writer: Arc<dyn DataWriter>,
      pub pipeline: PipelineConfig,
      pub record_builder: Arc<RecordBuilder>,
      pub reader_split: SplitReaderResult,
      pub stream_mode: StreamMode,
  }

  Planner 负责：

  JobConfig
    ├── 创建 Reader
    ├── 创建 Writer
    ├── 解析 PipelineConfig
    ├── 构建 RecordBuilder
    ├── 执行 reader.split()
    └── 生成 ExecutionPlan

  类似：

  pub struct Planner;

  impl Planner {
      pub async fn build(config: Arc<JobConfig>) -> Result<ExecutionPlan> {
          let reader = ReaderRegistry::instance()
              .prepare_reader(&config.source.source_type, Arc::clone(&config))?;

          let writer = WriterRegistry::instance()
              .prepare_writer(&config.target.source_type, Arc::clone(&config))?;
    
          let pipeline = PipelineConfig::from_system_config(&config);
    
          let source_type = config.source.source_type.parse()?;
    
          let record_builder = Arc::new(
              RecordBuilder::new(
                  config.column_mapping.clone(),
                  config.column_types.clone(),
              )?
              .with_source_type(source_type),
          );
    
          let reader = Arc::from(reader);
          let writer = Arc::from(writer);
    
          let reader_split = reader.split(pipeline.reader_threads).await?;
          let stream_mode = reader_split.stream_mode;
    
          Ok(ExecutionPlan {
              reader,
              writer,
              pipeline,
              record_builder,
              reader_split,
              stream_mode,
          })
      }
  }

  这里的关键是：

  reader.split()

  只在 Planner 阶段执行一次。

  ———

  # Engine 应该改成什么样

  当前：

  executor.run(reader, writer, job_config, cancel_token)

  建议改成：

  executor.run(plan, cancel_token)

  Trait：

  #[async_trait::async_trait]
  pub trait TaskExecutor: Send + Sync {
      async fn run(
          &self,
          plan: ExecutionPlan,
          cancel_token: CancellationToken,
      ) -> Result<RunResult>;
  }

  BatchRunner：

  let pipeline_stats = start_run(plan, cancel_token).await?;

  StreamRunner：

  let pipeline_future = start_run(plan, pipeline_token);

  这样 Engine 不再了解 JobConfig。

  ———

  # Pipeline 应该如何接收计划

  当前：

  start_run(
      config,
      reader,
      writer,
      job_config,
      cancel_token,
  )

  改成：

  pub async fn start_run(
      plan: ExecutionPlan,
      cancel_token: CancellationToken,
  ) -> Result<PipelineStats>

  或者为了避免 pipeline_executor 依赖 Planner 模块，也可以定义更窄的运行时参数：

  pub struct PipelineRunPlan {
      pub config: PipelineConfig,
      pub reader: Arc<dyn DataReader>,
      pub writer: Arc<dyn DataWriter>,
      pub record_builder: Arc<RecordBuilder>,
      pub reader_split: SplitReaderResult,
  }

  然后：

  pub async fn start_run(
      plan: PipelineRunPlan,
      cancel_token: CancellationToken,
  ) -> Result<PipelineStats>

  我更推荐第二种，因为：

  planner.rs      负责从 JobConfig 构建计划
  pipeline_executor.rs 只依赖运行时计划

  避免 pipeline_executor 和完整 ExecutionPlan 互相绑定。

  ———

  # 需要同步修改的 Pipeline 部分

  run_paired_pipeline 当前会重新 split：

  let reader_split = reader.split(config.reader_threads).await?;

  应该改为直接使用 Planner 已经生成的结果：

  let reader_split = plan.reader_split;

  然后 Pipeline 只负责：

  读取已规划的 ReadTask
  创建 Writer Task
  建立 Channel
  启动并发执行
  统计结果

  它不再负责：

  解析 JobConfig
  读取系统配置
  决定 Reader 类型
  创建 RecordBuilder
  执行 Reader split

  ———

  # 当前 Box/Arc 报错的根本原因

  你当前 Registry 返回：

  Result<Box<dyn DataReader>>

  而执行器需要：

  Arc<dyn DataReader>

  所以现在只能在 start_task 中转换：

  executor.run(Arc::from(reader), Arc::from(writer), ...)

  这能工作，但 Planner 重构时建议直接统一为：

  type ReaderCreator = fn(Arc<JobConfig>) -> Result<Arc<dyn DataReader>>;
  type WriterCreator = fn(Arc<JobConfig>) -> Result<Arc<dyn DataWriter>>;

  注册：

  create: |config| {
      Ok(Arc::new(DatabaseReader::init(config)?))
  }

  这样：

  let reader = ReaderRegistry::instance()
      .prepare_reader(...)?;

  let writer = WriterRegistry::instance()
      .prepare_writer(...)?;

  直接得到：

  Arc<dyn DataReader>
  Arc<dyn DataWriter>

  不再需要在 Runner 中补转换。

  ———

  # 这次重构的正确顺序

  建议按照这个顺序进行：

  ## 第一步：创建 Planner

  新增：

  core/src/core/planner.rs

  暂时只负责：

  - Reader/Writer 创建
  - PipelineConfig 构建
  - RecordBuilder 构建
  - Reader split
  - ExecutionPlan 生成

  ## 第二步：把 TaskExecutor::run 改成接收 ExecutionPlan

  从：

  run(reader, writer, job_config, cancel_token)

  变成：

  run(plan, cancel_token)

  ## 第三步：让 pipeline_executor 接收计划数据

  移除：

  job_config: Arc<JobConfig>

  移除内部：

  RecordBuilder::new(...)
  source_type.parse()

  ## 第四步：删除 Pipeline 内部重复 split

  改为使用 Planner 传入的 reader_split。

  ## 第五步：统一 Registry 的 Box/Arc 返回类型

  这一步可以和 Planner 一起做，也可以作为第二阶段优化。

  ———

  # 当前是否已经完成阶段四？

  还没有完全完成。

  现在已经完成的是：

  Reader/Writer 注册发现与懒加载

  但还没有完成：

  JobConfig → ExecutionPlan → Engine

  Registry 重构：已完成主要部分
  Planner 重构：尚未开始
  Engine 与配置解耦：尚未完成

  另外，我这一步暂时没有直接提交 Planner 代码，因为当前存在“Reader split 两次”的架构问题。如果只把 RecordBuilder 搬到
  Planner，而不同时处理 reader_split 的生命周期，会留下一个表面解耦、实际重复执行的问题。

# **基于当前**RELUS架构的问题分析起源：

• 你的方向是正确的：如果目标是做一个长期可扩展的 Rust 数据同步引擎，继续沿用纯 DataX 的：

  Reader → Framework → Writer

  会逐渐遇到扩展瓶颈。DataX 的模型适合“单任务、批处理、插件读取/写入”，但不适合很好地表达：

  - 多阶段转换
  - 流批统一
  - Checkpoint
  - 断点恢复
  - 多任务调度
  - 资源隔离
  - 复杂拓扑
  - 同步状态与一致性

  更合适的目标应该是：

  Source → Transform → Sink
                ↓
         Execution Engine

  但不建议直接照搬 SeaTunnel 或 Zeta Engine 的所有概念。应该吸收它们的边界思想，再使用 Rust 的类型系统、异步运行时和并发
  模型重新设计。

  ———

  ## 一、DataX 模型的问题

  当前模型大致是：

  Reader
    ├── split
    └── read_data

  Framework
    ├── 创建 Reader
    ├── 创建 Writer
    ├── 建立 Pipeline
    ├── 管理 Task
    └── 汇总结果

  Writer
    ├── split
    └── write_data

  这个模型的优点是简单：

  - Reader 负责读取
  - Writer 负责写入
  - Framework 负责连接两者
  - 插件扩展成本低

  但它把很多职责挤到了 Framework/Pipeline 中。

  例如未来要支持：

  数据库读取
    → 字段映射
    → 类型转换
    → 脱敏
    → 过滤
    → 分流
    → 批量写入

  当前可能会演变成：

  Reader → Pipeline 中写死转换逻辑 → Writer

  结果就是 Framework 越来越臃肿。

  另外，Reader 和 Writer 的抽象偏向“组件实现”，而不是“数据流节点”。这会导致：

  - Transform 没有自然位置
  - Checkpoint 难以定义
  - Sink 状态和 Source 位点难以统一
  - 流式和批式模型分裂
  - 任务调度依赖具体插件行为

  ———

  ## 二、应该吸收 SeaTunnel 的哪些思想

  SeaTunnel 最值得借鉴的不是类名，而是边界。

  ### 1. Connector 与执行引擎解耦

  Connector 只负责：

  如何连接外部系统
  如何读取或写入数据
  如何生成或恢复位点
  如何暴露能力

  Execution Engine 负责：

  如何调度
  如何并发
  如何传递数据
  如何做背压
  如何保存 Checkpoint
  如何故障恢复
  如何统计状态

  这意味着数据库 Connector 不应该自己决定：

  - 线程怎么创建
  - Channel 怎么连接
  - 任务什么时候重试
  - Checkpoint 存哪里
  - 失败后如何恢复

  这些应该由引擎统一决定。

  你的当前代码中，reader、writer 由 Registry 创建，runner 调度 Pipeline，这已经是一个不错的起点。下一步是不要让
  DataReader / DataWriter 同时承担过多执行职责。

  ### 2. Source / Transform / Sink 是数据流抽象

  建议将数据处理抽象为：

  Source<T>
  Transform<T, U>
  Sink<U>

  或者使用统一消息类型：

  Source → RecordBatch → Transform → RecordBatch → Sink

  对你的场景，统一类型可以先设计成：

  pub struct Record {
      pub fields: Vec<Value>,
      pub metadata: RecordMetadata,
  }

  或者保留动态 JSON：

  pub struct Record {
      pub payload: serde_json::Value,
      pub metadata: RecordMetadata,
  }

  不过不建议永久只使用 serde_json::Value。JSON 灵活，但会带来：

  - 类型检查弱
  - 转换成本高
  - 大批量场景内存开销大
  - 数据库原生类型信息丢失

  比较现实的演进路径是：

  第一阶段：serde_json::Value
  第二阶段：Record + TypedValue
  第三阶段：RecordBatch + 列式/批量数据

  ### 3. Engine 管理拓扑，而不是只连接 Reader 和 Writer

  未来配置可以表达：

  Source(database)
    → Transform(rename)
    → Transform(filter)
    → Transform(mask)
    → Sink(postgres)

  引擎根据配置构建 DAG：

  SourceNode
     ↓
  TransformNode
     ↓
  TransformNode
     ↓
  SinkNode

  即使第一版只支持线性 Pipeline，也建议内部模型按 DAG 设计：

  pub struct JobGraph {
      pub nodes: Vec<Node>,
      pub edges: Vec<Edge>,
  }

  这样以后增加分流、合流、广播时，不需要重新推翻核心模型。

  ———

  ## 三、建议的 Rust 分层

  可以将系统拆成五层：

  ┌────────────────────────────────────┐
  │             Job API / CLI          │
  ├────────────────────────────────────┤
  │          Job Planner / Compiler    │
  ├────────────────────────────────────┤
  │          Execution Engine          │
  │ scheduling / channel / retry       │
  │ checkpoint / metrics / recovery    │
  ├────────────────────────────────────┤
  │       Source / Transform / Sink    │
  ├────────────────────────────────────┤
  │       Connector / Runtime Adapter  │
  └────────────────────────────────────┘

  ### 1. Job API / CLI

  负责：

  - 加载 JSON/YAML/TOML
  - 校验配置
  - 提交任务
  - 查询任务状态
  - 停止任务

  这里不应该创建具体 Reader/Writer，也不应该直接管理 Tokio Task。

  ### 2. Job Planner

  负责把配置编译成执行计划：

  JobConfig
     ↓
  ValidatedJob
     ↓
  JobGraph
     ↓
  ExecutionPlan

  例如：

  pub struct ExecutionPlan {
      pub source: SourcePlan,
      pub transforms: Vec<TransformPlan>,
      pub sink: SinkPlan,
      pub parallelism: usize,
      pub checkpoint: CheckpointConfig,
  }

  Planner 负责检查：

  - Source 是否存在
  - Sink 是否存在
  - Transform 参数是否合法
  - Source 与 Sink 类型是否兼容
  - 是否开启事务
  - 并发度是否合法

  ### 3. Execution Engine

  这是未来的核心，不应再只是当前的 runner.rs。

  建议拆成：

  engine/
  ├── coordinator.rs
  ├── worker.rs
  ├── channel.rs
  ├── checkpoint.rs
  ├── retry.rs
  ├── state.rs
  ├── metrics.rs
  └── runtime.rs

  职责分别是：

  - Coordinator：任务生命周期、启动/停止、失败判定
  - Worker：执行 Source/Transform/Sink 节点
  - Channel：节点之间的数据传递和背压
  - Checkpoint：保存恢复点
  - Retry：重试策略
  - State：运行状态和算子状态
  - Metrics：吞吐、延迟、失败数
  - Runtime：Tokio task、取消、资源限制

  ### 4. Source / Transform / Sink

  建议这些是“逻辑算子”，而不是直接暴露线程模型。

  例如：

  #[async_trait]
  pub trait Source: Send + Sync {
      async fn open(&mut self, ctx: &SourceContext) -> Result<()>;
      async fn poll(&mut self, ctx: &mut SourceContext) -> Result<SourceMessage>;
      async fn snapshot_state(&self) -> Result<SourceState>;
      async fn close(&mut self) -> Result<()>;
  }

  #[async_trait]
  pub trait Transform: Send + Sync {
      async fn process(
          &mut self,
          record: Record,
          ctx: &TransformContext,
      ) -> Result<TransformOutput>;
  }

  #[async_trait]
  pub trait Sink: Send + Sync {
      async fn open(&mut self, ctx: &SinkContext) -> Result<()>;
      async fn write(&mut self, batch: RecordBatch) -> Result<()>;
      async fn prepare_commit(&mut self) -> Result<CommitToken>;
      async fn commit(&mut self, token: CommitToken) -> Result<()>;
      async fn abort(&mut self) -> Result<()>;
  }

  这里最重要的是：Source、Transform、Sink 的 trait 只描述能力，不描述具体线程创建方式。

  ———

  ## 四、Source、Transform、Sink 如何映射你现有代码

  当前结构可以这样迁移：

   当前模型                      新模型
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   DataReader                    Source
  ────────────────────────────  ──────────────────────────────────────────
   DataReaderJob::split          Source parallelism / partition discovery
  ────────────────────────────  ──────────────────────────────────────────
   DataReaderTask::read_data     Source task/operator
  ────────────────────────────  ──────────────────────────────────────────
   DataWriter                    Sink
  ────────────────────────────  ──────────────────────────────────────────
   DataWriterJob::split          Sink parallelism
  ────────────────────────────  ──────────────────────────────────────────
   DataWriterTask::write_data    Sink operator
  ────────────────────────────  ──────────────────────────────────────────
   PipelineMessage               RecordMessage
  ────────────────────────────  ──────────────────────────────────────────
   pipeline_executor.rs          Execution Engine
  ────────────────────────────  ──────────────────────────────────────────
   runner.rs                     Job Coordinator / Executor
  ────────────────────────────  ──────────────────────────────────────────
   ReaderRegistry                SourceRegistry
  ────────────────────────────  ──────────────────────────────────────────
   WriterRegistry                SinkRegistry

  不过不要简单做机械改名。

  例如当前：

  trait DataReaderJob
  trait DataReaderTask
  trait DataReader

  本质上把：

  逻辑 Source
  Source 分片
  Source 执行实例

  混在了一起。

  更清晰的设计是拆成：

  pub trait SourceConnector {
      type Source: Source;

      fn discover_partitions(
          &self,
          ctx: &PlanningContext,
      ) -> Result<Vec<Partition>>;
    
      fn create_source(
          &self,
          partition: Partition,
          config: Arc<JobConfig>,
      ) -> Result<Box<dyn Source>>;
  }

  这样：

  - Connector 负责发现分区和创建 Source
  - Engine 负责并发运行这些 Source
  - Source 负责产生数据

  这比让 DataReader::split() 同时决定任务数量、模式和执行流程更清楚。

  ———

  ## 五、Checkpoint 应该怎样设计

  这是从 DataX 模型升级到 SeaTunnel 模型时最重要的部分。

  ### Source Checkpoint

  数据库全量同步：

  offset = page number / primary key range

  Binlog：

  offset = binlog file + position

  API：

  offset = cursor / page token

  所以不要把 checkpoint 写成固定字段：

  pub struct Checkpoint {
      pub offset: usize,
  }

  应该定义为可扩展状态：

  pub struct OperatorState {
      pub operator_id: String,
      pub payload: serde_json::Value,
  }

  或者：

  pub trait CheckpointState:
      Send + Sync + Serialize + DeserializeOwned
  {
  }

  不同 Connector 保存自己的状态。

  ### Sink Checkpoint

  Sink 需要考虑：

  - 已经写入哪些批次
  - 哪个事务已经提交
  - 重试是否会重复写入
  - 是否支持幂等写入
  - 是否支持两阶段提交

  第一版不建议直接实现完整的两阶段提交。可以先支持：

  至少一次（At-Least-Once）
  + 幂等 Sink
  + operation_id / batch_id

  例如：

  pub struct WriteBatchId {
      pub job_id: String,
      pub checkpoint_id: u64,
      pub partition_id: u32,
  }

  数据库 Sink 可以使用：

  - 主键 Upsert
  - 唯一批次号
  - 操作日志表
  - 已提交批次表

  后续再考虑 Exactly-Once。

  ———

  ## 六、故障恢复应该由 Engine 负责

  不要让每个 Connector 自己实现一套重试和恢复。

  建议统一定义：

  pub enum FailurePolicy {
      FailFast,
      Retry {
          max_attempts: u32,
          backoff: Duration,
      },
      Skip,
  }

  引擎负责：

  Source 失败
    ↓
  判断错误是否可恢复
    ↓
  重试当前算子
    ↓
  恢复最近 Checkpoint
    ↓
  继续执行

  Connector 只需提供错误分类：

  pub enum ConnectorErrorKind {
      Transient,
      Authentication,
      InvalidConfig,
      DataError,
      Fatal,
  }

  如果把重试写在 MySQL Reader、Postgres Writer、API Reader 各自内部，最终会出现行为不一致。

  ———

  ## 七、Rust 特别适合的地方

  Rust 不一定要模仿 SeaTunnel 的 Java 线程模型。

  ### 1. Tokio task + 有界 channel

  每个算子可以运行在异步任务中：

  Source task
      ↓ bounded channel
  Transform task
      ↓ bounded channel
  Sink task

  有界 channel 自带背压：

  let (tx, rx) = tokio::sync::mpsc::channel(1024);

  不要一开始就使用无界队列，否则大表同步时容易出现内存增长。

  ### 2. 不要让每条记录都跨越过多抽象层

  推荐批量消息：

  pub enum RecordMessage {
      Data(RecordBatch),
      Barrier(CheckpointBarrier),
      Watermark(Watermark),
      End,
  }

  而不是：

  Record
  Record
  Record
  Record

  批量处理更适合数据库同步。

  ### 3. 用类型表达生命周期和状态

  例如：

  pub enum TaskStatus {
      Created,
      Planning,
      Running,
      Checkpointing,
      Succeeded,
      Failed,
      Cancelled,
  }

  而不是到处传字符串：

  "running"
  "success"
  "failed"

  ### 4. 使用 cancellation token

  你当前已经使用：

  CancellationToken

  这是正确方向。建议将它纳入统一的 TaskContext：

  pub struct TaskContext {
      pub task_id: String,
      pub cancellation: CancellationToken,
      pub metrics: Arc<MetricsRegistry>,
  }

  ———

  ## 八、Registry 应该怎么演进

  你目前的：

  ReaderRegistry
  WriterRegistry

  可以演进为：

  SourceRegistry
  TransformRegistry
  SinkRegistry

  但是不要只改名称，注册对象也应该从“具体实现工厂”升级为“Connector 描述”。

  例如：

  pub struct SourceFactory {
      pub name: &'static str,
      pub capabilities: SourceCapabilities,
      pub create: fn(SourceConfig) -> Result<Box<dyn Source>>,
  }

  pub struct SourceCapabilities {
      pub supports_batch: bool,
      pub supports_streaming: bool,
      pub supports_checkpoint: bool,
      pub supports_parallelism: bool,
  }

  这样 Planner 可以在运行前判断：

  该 Source 是否支持流式？
  是否支持并发分片？
  是否支持断点恢复？

  而不是运行到一半才发现能力不支持。

  对于新增插件，最终目标应是：

  新增 source 模块
  注册 SourceFactory
  无需修改 Engine
  无需修改 Planner 主逻辑

  如果继续使用 inventory，它仍然可以作为编译期插件发现机制。但建议把它封装在 Registry 内部，不让 Engine 直接感知
  inventory。

  ———

  ## 九、建议的演进路线

  不要一次性把现有 DataX 架构全部推翻。建议分阶段。

  ### 阶段一：稳定现有执行边界

  保留：

  Reader → Pipeline → Writer

  先完成：

  - Reader/Writer 懒加载注册
  - Box/Arc 所有权统一
  - 统一任务状态
  - 统一错误类型
  - 统一指标
  - 统一操作记录
  - 统一取消机制

  这一阶段主要是治理基础设施。

  ### 阶段二：引入统一 RecordMessage

  把当前的 PipelineMessage 固化为：

  pub enum RecordMessage {
      Data(RecordBatch),
      Barrier(CheckpointBarrier),
      End,
  }

  即使暂时没有 Transform，也先让 Reader 和 Writer 通过统一消息通信。

  ### 阶段三：加入 Transform 节点

  把字段映射、类型转换、过滤、脱敏从 Pipeline 中移出：

  Reader → MappingTransform → Writer

  此时开始真正形成：

  Source → Transform → Sink

  ### 阶段四：引入 Planner

  将配置解析和执行分离：

  JobConfig → ExecutionPlan → Engine

  不让 Engine 直接理解完整 JSON 配置。

  ### 阶段五：Checkpoint 和恢复

  先做：

  At-Least-Once
  + Source checkpoint
  + 幂等 Sink
  + 任务重启恢复

  再考虑：

  Exactly-Once
  + 两阶段提交

  ### 阶段六：调度与资源管理

  最后再完善：

  - 并发度
  - 任务队列
  - 资源限额
  - 多任务调度
  - 优先级
  - Worker 管理
  - 分布式执行

  不要在单机同步引擎尚未稳定时，先实现完整 Zeta 风格资源调度。

  ———

  ## 十、推荐的目标架构

  可以把最终目标抽象成：

                      ┌──────────────┐
                      │  JobConfig   │
                      └──────┬───────┘
                             ↓
                      ┌──────────────┐
                      │ Job Planner  │
                      └──────┬───────┘
                             ↓
                      ┌──────────────┐
                      │ ExecutionPlan│
                      └──────┬───────┘
                             ↓
                ┌────────────────────────┐
                │    Execution Engine    │
                │                        │
                │ Coordinator             │
                │ Scheduler               │
                │ Channel / Backpressure │
                │ Checkpoint              │
                │ Retry / Recovery       │
                │ Metrics                 │
                └──────┬─────────┬───────┘
                       ↓         ↓
                   Source     Transform
                       ↓         ↓
                       └────┬────┘
                            ↓
                           Sink

  插件边界：

  Source Connector
  Transform Connector
  Sink Connector

  执行引擎不应该知道 MySQL、Postgres、Kafka 的细节；Connector 也不应该知道 Tokio task 如何调度。

  ## 最终判断

  你的想法不是简单地“把 DataX 翻译成 Rust”，而是要从：

  插件驱动的数据搬运框架

  升级为：

  可编排、可恢复、可扩展的数据流执行引擎

  最关键的变化有三个：

  1. Reader/Writer 升级为 Source/Transform/Sink
  2. Framework 升级为独立的 Execution Engine
  3. split/read/write 升级为分区、算子、状态和消息协议

  但建议保持渐进式演进：

  先治理现有 Reader/Writer
  → 统一消息和状态
  → 加入 Transform
  → 引入 Planner
  → 加入 Checkpoint
  → 最后扩展调度和资源管理

  否则一次性引入 SeaTunnel/Zeta 的所有模型，很容易出现抽象过度、接口复杂、功能却无法稳定落地的问题。